# WebCube2027 架構概覽

> 最後更新：2026-07-20（新增規格書優先注意事項）

---

## 專案定位

WebCube2027 是一個多閘道（Multi-Gateway）架構的 Web 應用平台，採用 Deno 2 + Hono 技術棧。核心設計理念是將不同的業務領域拆分為獨立的 Gateway 服務，共享底層的 Packages 層。

---

## 整體架構

```
packages/          ← 共用套件層（跨所有 gateway）
gateway/           ← 業務閘道層（獨立 HTTP 服務）
main.ts            ← 根入口（開發用單一啟動點）
deno.json          ← workspace 根設定 + 共用 imports
```

> **📖 規格書優先**：每個 Package 與 Gateway 的根目錄皆包含 `docs/規格書.md`，詳細說明該模組的架構設計、API 定義與使用方式。若有任何疑問，請先查閱對應的規格書。

### 三層資料流

```
[其他 Gateway] ──HTTP/InnerAPI──→ [data-gateway] ──@dui/database──→ [Database]
```

---

## Packages（共用層）

| Package | 名稱 | 版本 | 職責 |
|---------|------|------|------|
| **dui-database** | `@dui/database` | 0.2.0 | 資料庫抽象層，支援 9 種 adapter（SurrealDB、SQLite、MongoDB、MySQL、PostgreSQL、Firestore、Appwrite、DynamoDB、MSSQL） |
| **dui-framework** | `@dui/framework` | 0.1.0 | 共用框架依賴管理（hono、fileRouter、tailwindcss、daisyUI），5 個管理型 Gateway 共用 |
| **dui-multilanguage** | `@dui/smartmultilingual` | 0.3.0 | 多國語言資料型態（MultilingualString、SmartContent），含翻譯服務介面（預設 Google Translate），可注入自訂翻譯實作 |
| **dui-translation** | `@dui/translation` | 0.1.0 | **籌備中** — AI 驅動的翻譯服務（AI → data-gateway 快取 → Google fallback），待 ai-gateway 完成後實作 |
| **dui-util** | `@dui/util` | 0.1.0 | 通用工具（InnerAPI 內部 API 代理、Logger 日誌記錄） |

### Package 設計原則

- **重複性高的基礎設施** → package（如資料庫、多國語言）
- **特定 gateway 的工具** → 留在自己的 `utils/` 目錄
- **Package 之間的依賴**透過 workspace 本地解析，不經過遠端 JSR

---

## Gateway（業務閘道層）

| Gateway | 埠號 | 狀態 | 職責 |
|---------|------|------|------|
| **data-gateway** | 8002 | ✅ 已建立 | 資料中心，提供 RESTful CRUD API，封裝 `@dui/database` |
| **ai-gateway** | — | 🔧 既有骨架 | AI 池，管理 LLM 調度，提供 OpenAI 相容 API |
| **render-gateway** | — | 🔧 既有 | 方塊渲染引擎（UnoCSS），尚未拆分 |
| **page-gateway** | — | 📦 待拆分 | 網頁中心，處理 AI 產生的網頁 |
| **cube-gateway** | — | 📦 待拆分 | 元件中心，處理 AI 產生的方塊 |
| **theme-gateway** | — | 📦 待拆分 | 佈景主題中心 |
| **media-gateway** | — | ❌ 規劃中 | 圖片/影片處理中心 |
| **排程閘道** | — | ❌ 規劃中 | 工作排程中心 |
| **auth-gateway** | — | ❌ 規劃中 | 認證中心，統一管理各 gateway 的登入/驗證 |

### Gateway 設計原則

- 每個 Gateway 是獨立的 HTTP 服務
- 透過 `InnerAPI`（`@dui/util`）進行內部服務間通訊
- 管理型 Gateway（ai、page、cube、theme、media）共用 `@dui/framework`（hono + tailwindcss + daisyUI + fileRouter）
- render-gateway 例外，使用 UnoCSS 渲染引擎

---

## 技術棧

### 共用 Runtime

| 技術 | 版本 | 來源 |
|------|------|------|
| Deno | 2.x | — |
| Hono | ^4.12.31 | `jsr:@hono/hono` |
| JSX | precompile | `hono/jsx` |

### 管理型 Gateway（ai / page / cube / theme）

| 技術 | 用途 |
|------|------|
| tailwindcss v4 | CSS 框架 |
| daisyUI v5 | UI 元件庫 |
| fileRouter（自製） | 檔案式路由 |

### render-gateway（專用）

| 技術 | 用途 |
|------|------|
| UnoCSS | 原子化 CSS 引擎（wind3 preset） |
| deno-dom | DOM 解析 |
| svgson | SVG 解析 |

### 資料庫支援（@dui/database）

| Adapter | 類型 |
|---------|------|
| SurrealDB | L2/L3 等級資料庫 |
| SQLite | 檔案型 |
| MongoDB | 文件型 |
| MySQL | 關聯式 |
| PostgreSQL | 關聯式 |
| Firestore | 雲端 NoSQL |
| Appwrite | 雲端 BaaS |
| DynamoDB | AWS NoSQL |
| MSSQL | Microsoft SQL Server |

---

## 主目錄 `main.ts`

根目錄的 `main.ts` 是開發階段的統一啟動點，負責：

1. 呼叫 `設定App()` 註冊 InnerAPI 實例
2. 啟動各 Gateway 服務
3. 設定全域中介軟體（CORS 等）

---

## Workspace 結構

```
deno.json（根目錄）
├── workspace：
│   ├── packages/dui-database
│   ├── packages/dui-framework
│   ├── packages/dui-multilanguage
│   ├── packages/dui-translation
│   ├── packages/dui-util
│   ├── gateway/data-gateway
│   ├── gateway/ai-gateway
│   ├── gateway/render-gateway
│   ├── gateway/page-gateway
│   ├── gateway/cube-gateway
│   └── gateway/theme-gateway
│
├── imports（所有 workspace 成員共享）：
│   ├── hono
│   ├── hono/serve-static
│   ├── hono/jsx
│   ├── hono/jsx/dom
│   └── hono/utils/html
│
└── compilerOptions：
    ├── jsx: precompile
    └── jsxImportSource: hono/jsx
```

---

## 開發指令

| 指令 | 說明 |
|------|------|
| `deno task dev` | 啟動所有服務（開發模式） |
| `deno task dev:ai` | 啟動 ai-gateway |
| `deno task dev:render` | 啟動 render-gateway |
| `deno task dev:data` | 啟動 data-gateway |
| `deno task start` | 正式啟動 |

---

## 未來規劃

1. **auth-gateway** — 統一認證服務
2. **media-gateway** — 媒體檔案處理
3. **排程閘道** — 工作排程服務
4. **Hermes 整合** — 將 AI agent 層移交給 Hermes，精簡 render-gateway 的 AI 程式碼
5. **@dui/translation 實作** — 待 ai-gateway 完成後，實作 AI 翻譯（含 data-gateway 快取）
6. **render-gateway 拆分** — 將 page、cube、theme 等服務逐步抽出
